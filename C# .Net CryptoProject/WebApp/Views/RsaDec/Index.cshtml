@model IEnumerable<Domain.RsaDecDb>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.n)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.d)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plaintext)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ciphertext)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.n)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.d)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plaintext)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ciphertext)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RsaDecDbId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RsaDecDbId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RsaDecDbId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
